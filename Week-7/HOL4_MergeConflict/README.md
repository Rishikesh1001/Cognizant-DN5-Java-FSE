# HOL4 - Merge Conflict

## Objective
Learn how to create and resolve a Git merge conflict.

## Commands Used

```bash
git init
git branch feature
git checkout feature
git merge feature
git add .
git commit -m "Resolved merge conflict"
```

## Result

Successfully resolved a merge conflict by editing the conflicting file and committing the changes.
